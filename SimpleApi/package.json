{
  "name": "simpleapi",
  "version": "1.0.0",
  "description": "Simple API",
  "main": "http.js",
  "scripts": {
    "dev:1": "nodemon http.js",
    "dev:2": "nodemon routing.js",
    "dev:3": "node --watch ./express/1.express.js",
    "dev:4": "node --watch ./express/2.express.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "standard": "^17.1.0"
  },
  "eslintConfig": {
    "extends": [
      "standard"
    ],
    "rules": {
      "prefer-promise-reject-errors": "off",
      "space-before-function-paren": [
        "error",
        "never"
      ]
    }
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
